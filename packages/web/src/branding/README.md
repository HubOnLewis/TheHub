# Hub CRM Branding (foundation)

Logo, wordmark, and app icon are **not finalized**. Current sidebar wordmark is legacy.

## Planned assets

- Premium wordmark + monogram
- Favicon / PWA icon
- Owner presentation deck theme (matches `premium-command-system.css`)

## Tokens

Use `tokens.ts` for typography, spacing, and palette references in new components.
