import { useQuery } from '@tanstack/react-query';
import client from '../api/client.js';
export default function ContactsPage(){const q=useQuery({queryKey:['contacts'],queryFn:()=>client.get('/contacts',{params:{limit:100}}).then(r=>r.data)});if(q.isLoading)return <main className="page-simple"><div className="card page-section">Loading contacts…</div></main>;if(q.isError)return <main className="page-simple"><div className="card page-section"><h1>Contacts</h1><p>Contacts are temporarily unavailable.</p></div></main>;const rows=q.data?.data||[];return <main className="page-simple"><header className="page-header"><div><span className="page-kicker">Customer relationships</span><h1 className="page-title">Contacts</h1><p className="page-subtitle">{q.data?.total||0} persisted contacts from the Hub workspace.</p></div></header><section className="card list-card"><div className="data-table-wrap"><table className="data-table"><thead><tr><th>Name</th><th>Email</th><th>Company</th><th>Source</th></tr></thead><tbody>{rows.map((x:any)=><tr key={x.id}><td>{x.displayName}</td><td>{x.email||'—'}</td><td>{x.companyName||'—'}</td><td>{x.source}</td></tr>)}</tbody></table></div></section></main>}
