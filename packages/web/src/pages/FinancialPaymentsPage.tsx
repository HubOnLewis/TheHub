import { useQuery } from '@tanstack/react-query';
import { formatCurrency } from '@hub-crm/shared';
import client from '../api/client.js';
export default function FinancialPaymentsPage(){const q=useQuery({queryKey:['financial','payments'],queryFn:()=>client.get('/financial/payments').then(r=>r.data)});if(q.isLoading)return <main className="page-simple"><div className="card page-section">Loading imported payments…</div></main>;if(q.isError)return <main className="page-simple"><div className="card page-section"><h1>Imported payments</h1><p>Financial records are temporarily unavailable.</p></div></main>;const rows=q.data?.data||[];return <main className="page-simple"><header className="page-header"><div><span className="page-kicker">Perfect Venue financial truth</span><h1 className="page-title">Imported payments</h1><p className="page-subtitle">{rows.length} imported transactions. Hub payment links remain a separate workflow.</p></div></header><section className="card list-card"><div className="data-table-wrap"><table className="data-table"><thead><tr><th>Event</th><th>Amount</th><th>Type</th><th>Status</th><th>Paid</th></tr></thead><tbody>{rows.map((x:any)=><tr key={x.id}><td>{x.eventTitle||'—'}</td><td>{formatCurrency(x.amount)}</td><td>{x.paymentType}</td><td>{x.status}</td><td>{x.paymentDate?new Date(x.paymentDate).toLocaleDateString():'—'}</td></tr>)}</tbody></table></div></section></main>}
