import LiveOwnerBriefingPage from './live/LiveOwnerBriefingPage.js';

/** Owner briefing from live / imported venue events — always intelligence, never fake metrics. */
export default function OwnerBriefing() {
  return <LiveOwnerBriefingPage />;
}
